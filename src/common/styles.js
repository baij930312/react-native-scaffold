import { StyleSheet} from 'react-native';

export let commonStyles =   StyleSheet.create({
    //横向布局
    rowContainer: {
        flexDirection:'row',
        alignItems:'center',
    },
    //纵向布局
    columnContainer: {
        flexDirection:'column',
    },
});
